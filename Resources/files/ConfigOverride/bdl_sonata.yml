sonata_user:
    security_acl: true
    manager_type: orm # can be orm or mongodb
    class:
        user:  Tact\CoreBundle\Entity\User
        group: Tact\CoreBundle\Entity\Group
    admin:
        user:
          class: Tact\CoreBundle\Admin\Model\UserAdmin

sonata_admin:
    title: eSoftLink Manager
    dashboard:
        blocks:
            -
                position: right
                type: sonata.block.service.text
                settings:
                    content: >
                        <h2>Welcome to the eSoftLink Admin</h2>
                        <p>This is a manager of Production data.</p> <br />
            -
                position: left
                type: sonata.admin.block.admin_list
            -
                position: right
                type: sonata.block.service.rss

    security:
        handler: sonata.admin.security.handler.role
        # acl security information
        information:
            GUEST:    []
            STAFF:    [EDIT, LIST, CREATE]
            EDITOR:   [EXPORT]
            ADMIN:    [MASTER]
            OWNER:    [OWNER]
        # permissions not related to an object instance and also to be available when objects do not exist
        # the DELETE admin permission means the user is allowed to batch delete objects
        admin_permissions: [CREATE, LIST, DELETE, UNDELETE, EXPORT, MASTER]
        # permission related to the objects
        object_permissions: [VIEW, EDIT, DELETE, UNDELETE, MASTER, OWNER]
